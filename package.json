{
  "name": "atomic-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3.14"
  },
  "contributors": [
    "Norin Lavaee",
    "Alex Lavaee"
  ],
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "bun scripts/install-hooks.mjs",
    "hooks:install": "bunx --bun --no-install prek install --prepare-hooks",
    "hooks:run": "bunx --bun --no-install prek run --all-files",
    "test": "bun run test:unit",
    "test:unit": "bun test --timeout 30000 test/unit",
    "test:integration": "bun test --timeout 30000 test/integration",
    "test:ci-contracts": "bun test --timeout 30000 test/ci",
    "test:all": "bun run test:unit && bun run test:integration",
    "typecheck": "tsc --noEmit",
    "check:shrinkwrap": "bun run scripts/generate-coding-agent-shrinkwrap.mjs --check",
    "shrinkwrap:coding-agent": "bun run scripts/generate-coding-agent-shrinkwrap.mjs",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@j178/prek": "0.4.10",
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  },
  "overrides": {
    "@types/node": "24.12.4",
    "protobufjs": "7.6.5",
    "brace-expansion": "5.0.7"
  }
}
